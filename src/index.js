import React from 'react';
import ReactDOM from 'react-dom';
//Main APP components where everything is rendered
import App from './App';

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('root')
);
